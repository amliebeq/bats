import { useEffect, useState } from 'react'
import './App.css'

function App() {
  // const [user, setUser] = useState('')

  // useEffect (() => {
  //   fetch('/api/users/1') .then((r) => {if (r.ok) {r.json().then ((user) => setUser(user))}})
  // },[])

  return (
    <div>
      {/* {user.first_name} {user.last_name} */}
    </div>
  )
}

export default App
